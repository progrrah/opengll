#include <GL/glew.h>
#include <GL/freeglut.h>
#include <vector>
#include <iostream>
#include <string>
// #include<cstdlib>

#include "other_genius_thing.hpp"
#include "stuff.hpp"
// using namespace objects;

int main(int argc, char** argv) {
  // glutDisplayFunc(myDisp);
  // glutKeyboardFunc(myKeyboardMain);
  // glutMouseFunc(myMouse);
  // glutReshapeFunc(myReshape);

  // glutMainLoop();
  // exit(EXIT_SUCCESS);
  std::vector<int> v1(3, 0);
  
  return 0;
}
